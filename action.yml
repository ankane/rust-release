name: Rust Release
inputs:
  cross:
    description: Use cross for Linux targets
  name:
    description: Set name
  version:
    description: Set version
  path:
    description: Set path
  target:
    description: Set target
  files:
    description: Set files
  manual-licenses-path:
    description: Set manual licenses path
runs:
  using: node16
  main: index.js
